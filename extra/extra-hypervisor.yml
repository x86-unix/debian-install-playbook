---
# KVM hypervisor + virt-manager + noVNC (HTTP, no auth)
# Access: http://<host>/

- name: "Extra: KVM Hypervisor (virt-manager + noVNC)"
  hosts: hypervisor
  gather_facts: false

  tasks:
    # ── CPU 仮想化支援チェック ─────────────────────────────────────────────────
    - name: Check CPU virtualization support
      ansible.builtin.shell: grep -cE '(vmx|svm)' /proc/cpuinfo
      register: virt_check
      changed_when: false
      failed_when: false

    - name: Fail if no virtualization support
      ansible.builtin.fail:
        msg: "CPU 仮想化支援（VT-x/AMD-V）が検出されません。BIOS/UEFI で有効にしてください。"
      when: virt_check.rc != 0

    # ── KVM + virt-manager ────────────────────────────────────────────────────
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
      failed_when: false
      changed_when: false

    - name: Install KVM and virt-manager
      ansible.builtin.apt:
        name:
          - qemu-kvm
          - libvirt-daemon-system
          - libvirt-clients
          - virtinst
          - virt-manager
        state: present

    - name: Add default_user to libvirt group
      ansible.builtin.user:
        name: "{{ default_user }}"
        groups: libvirt
        append: true

    - name: Enable and start libvirtd
      ansible.builtin.systemd:
        name: libvirtd
        enabled: true
        state: started

    # ── ブリッジネットワーク（LAN 内から VM に直接アクセス可能） ───────────────
    - name: Detect primary network interface
      ansible.builtin.shell: ip route show default | awk '/default/ {print $5; exit}'
      register: primary_nic
      changed_when: false

    - name: Get current connection name for primary NIC
      ansible.builtin.shell: nmcli -t -f NAME,DEVICE con show --active | grep ':{{ primary_nic.stdout }}$' | cut -d':' -f1
      register: current_conn
      changed_when: false

    - name: Check if bridge br0 already exists
      ansible.builtin.shell: nmcli -t -f NAME con show | grep -q '^br0$'
      register: br0_exists
      changed_when: false
      failed_when: false

    - name: Create bridge br0
      ansible.builtin.shell: |
        nmcli con add type bridge ifname br0 con-name br0
        nmcli con modify br0 ipv4.dhcp-client-id mac
        nmcli con add type bridge-slave ifname {{ primary_nic.stdout }} master br0
        nmcli con down "{{ current_conn.stdout }}" || true
        nmcli con up br0
      when: br0_exists.rc != 0

    - name: Define libvirt bridged network
      ansible.builtin.copy:
        dest: /tmp/br0-net.xml
        mode: "0644"
        content: |
          <network>
            <name>br0</name>
            <forward mode="bridge"/>
            <bridge name="br0"/>
          </network>
      when: br0_exists.rc != 0

    - name: Check if libvirt br0 network exists
      ansible.builtin.shell: virsh net-list --all --name | grep -q '^br0$'
      register: libvirt_br0_exists
      changed_when: false
      failed_when: false

    - name: Register libvirt bridged network
      ansible.builtin.shell: |
        virsh net-define /tmp/br0-net.xml
        virsh net-start br0
        virsh net-autostart br0
      when: libvirt_br0_exists.rc != 0

    # ── wayvnc / noVNC ────────────────────────────────────────────────────────
    - name: Install wayvnc, novnc, websockify and apache2
      ansible.builtin.apt:
        name:
          - wayvnc
          - novnc
          - websockify
          - apache2
        state: present

    - name: Ensure wayvnc config directory
      ansible.builtin.file:
        path: /home/{{ default_user }}/.config/wayvnc
        state: directory
        owner: "{{ default_user }}"
        mode: "0700"

    - name: Configure wayvnc (localhost only, no auth)
      ansible.builtin.copy:
        dest: /home/{{ default_user }}/.config/wayvnc/config
        owner: "{{ default_user }}"
        group: "{{ default_user }}"
        mode: "0600"
        content: |
          address=127.0.0.1
          enable_auth=false

    - name: Add wayvnc to labwc autostart
      ansible.builtin.lineinfile:
        path: /home/{{ default_user }}/.config/labwc/autostart
        line: "wayvnc &"
        create: true
        owner: "{{ default_user }}"
        group: "{{ default_user }}"
        mode: "0755"

    - name: Create novnc systemd service
      ansible.builtin.copy:
        dest: /etc/systemd/system/novnc.service
        mode: "0644"
        content: |
          [Unit]
          Description=noVNC WebSocket bridge
          After=network.target

          [Service]
          ExecStart=/usr/bin/websockify --web /usr/share/novnc 127.0.0.1:6080 127.0.0.1:5900
          Restart=on-failure

          [Install]
          WantedBy=multi-user.target

    - name: Enable and start novnc service
      ansible.builtin.systemd:
        name: novnc
        enabled: true
        state: started
        daemon_reload: true

    # ── Apache HTTP リバースプロキシ ──────────────────────────────────────────
    - name: Enable required Apache modules
      ansible.builtin.command: a2enmod {{ item }}
      loop:
        - proxy
        - proxy_http
        - proxy_wstunnel
        - rewrite
      changed_when: false

    - name: Disable default Apache site
      ansible.builtin.command: a2dissite 000-default
      changed_when: false

    - name: Deploy noVNC Apache vhost (HTTP, no auth)
      ansible.builtin.copy:
        dest: /etc/apache2/sites-available/novnc.conf
        mode: "0644"
        content: |
          Listen 0.0.0.0:80

          <VirtualHost *:80>
              ProxyPass /websockify ws://localhost:6080/websockify
              ProxyPassReverse /websockify ws://localhost:6080/websockify
              ProxyPass / http://localhost:6080/
              ProxyPassReverse / http://localhost:6080/

              RewriteEngine On
              RewriteRule ^/$ /vnc.html [R,L]
          </VirtualHost>

    - name: Enable noVNC Apache site
      ansible.builtin.command: a2ensite novnc
      changed_when: false

    - name: Enable and start Apache
      ansible.builtin.service:
        name: apache2
        enabled: true
        state: started
